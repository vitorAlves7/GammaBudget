import { MonthYearFilterPipe } from './month-year-filter.pipe';

describe('MonthYearFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthYearFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
