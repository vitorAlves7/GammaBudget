import { MonthSelectorComponent } from './month-selector.component';

describe('MonthSelectorComponent', () => {
  let component: MonthSelectorComponent;

  beforeEach(() => {
    component = new MonthSelectorComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
